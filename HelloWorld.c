#include <stdio.h> //including the standard input/output library to get printf

// this is a comment

// the main function where the execution of the program begins
int main()
{
    printf("hello world!");
    // 0 default value means that program executed successfully
    return 0;
}